services:
  kudosbot:
    image: kudosbot
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7212:7212"
    volumes:
      - .:/app
    command: uvicorn src.main:app --host 0.0.0.0 --port 7212 --env-file .env
